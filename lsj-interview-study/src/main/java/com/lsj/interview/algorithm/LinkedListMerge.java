package com.lsj.interview.algorithm;

import java.util.LinkedList;

public class LinkedListMerge {
    public static void merge(LinkedList<Integer> a, LinkedList<Integer> b) {
        int aSize = a.size();
        int bSize = b.size();
        int i = 0, j = 0;
        while (i < aSize && j < bSize) {
            if (a.get(i) < b.get(j)) {

            }
        }
    }
}
